node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# heavy AI-generated 3D intermediates (regenerable; use Git LFS if needed later)
# — only the two 100MB+ scene bundles are excluded; the rest of 3d/ uploads
blender/3d/cottagecore-scene-final.glb
blender/3d/cottagecore-scene-night.glb
blender/.tmp/

# Blender project sources — kept local / cloud backup, too big for GitHub
*.blend
*.blend1
